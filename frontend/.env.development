# Development environment - used by `npm start` (local)
# CRA dev server proxies all /api/* requests to localhost:5000 (see "proxy" in package.json)
# REACT_APP_API_URL is listed here for reference but axios does NOT use it as baseURL in dev
REACT_APP_API_URL=http://localhost:5000